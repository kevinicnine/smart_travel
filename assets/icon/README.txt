Put your app icon here as app_icon.png (recommended 1024x1024). After placing the file, run:

flutter pub get
flutter pub run flutter_launcher_icons

Current config in pubspec.yaml expects the icon at assets/icon/app_icon.png.
